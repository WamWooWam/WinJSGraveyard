import * as Windows from "../../../src/Windows"
const WebUi = Windows.UI.WebUI;

export class ApplicationLifetimeManager {
    raiseActivated(arg: any): void {
        
    }

    raiseResuming(): void {
        
    }

    raiseSuspending(): void {
        
    }
}